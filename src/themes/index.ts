/**
 * codepane — Theme Presets
 *
 * Ready-to-use theme presets for the editor. Each export is a complete
 * `EditorTheme` object that can be passed directly to `Editor.Root` or
 * used as a base for further customization via `mergeTheme()`.
 */

export { zedDark } from './zed-dark'
export { zedLight } from './zed-light'
